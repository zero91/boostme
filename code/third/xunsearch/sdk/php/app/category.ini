project.name = category
project.default_charset = utf-8
server.index = 8383
server.search = 8384

[id]
type = id

[region_id]
index = self
tokenizer = full

[region]
index = both
tokenizer = default

[school_id]
index = self
tokenizer = full

[school]
index = both
tokenizer = default

[dept_id]
index = self
tokenizer = full

[dept]
index = both
tokenizer = default

[major_id]
index = self
tokenizer = full

[major]
index = both
tokenizer = default
